-- ============================================
-- PROJETO KILL V4 - MULTI-SELECTION (OMNI ORBIT) - CORRIGIDO
-- ============================================
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local StarterGui = game:GetService("StarterGui")

local lp = Players.LocalPlayer
local character = lp.Character or lp.CharacterAdded:Wait()
local camera = Workspace.CurrentCamera
local playerGui = lp:WaitForChild("PlayerGui")

-- --- VARIÁVEIS DE ESTADO ---
local modoAtivo = false
local objetosSelecionados = {}
local conexaoAtaque = nil
local posicaoOriginal = nil
local bypassEmAndamento = false
local aguardandoRetorno = false
local toolSelecionada = nil
local botaoCriado = false

-- --- ATUALIZAR REFERÊNCIA DO CHARACTER ---
local function atualizarCharacter(newChar)
    character = newChar
    posicaoOriginal = nil
    bypassEmAndamento = false
    aguardandoRetorno = false
end

lp.CharacterAdded:Connect(atualizarCharacter)

-- --- TRANCAR AUTORIDADE DE REDE (SIMULATION RADIUS MÁXIMO) ---
local sethp = sethiddenproperty or set_hidden_property
if sethp then
    task.spawn(function()
        RunService.RenderStepped:Connect(function()
            pcall(function()
                sethp(lp, "SimulationRadius", 1e30)
                sethp(lp, "MaxSimulationRadius", 1e30)
            end)
        end)
    end)
end

-- --- VERIFICAÇÃO ESPECÍFICA DE HELICÓPTEROS ---
local function ehHelicoptero(instancia)
    if not instancia then return false end
    
    local model = instancia:IsA("Model") and instancia or instancia:FindFirstAncestorOfClass("Model")
    if not model then return false end
    
    local nome = string.lower(model.Name)
    local padroesHeli = {
        "chinook", "heli", "copter", "helicopter", "chopper",
        "uh-60", "uh60", "ah-64", "ah64", "apache", "blackhawk",
        "bell", "robinson", "ec-145", "ec145", "h145", "md-500",
        "md500", "bo-105", "bo105", "enstrom", "sikorsky", "eurocopter",
        "airbus", "agusta", "westland", "mi-24", "mi24", "hind",
        "osprey", "v-22", "v22", "merlin", "lynx", "gazelle"
    }
    
    for _, padrao in pairs(padroesHeli) do
        if string.find(nome, padrao) then
            return true
        end
    end
    
    if model:FindFirstChild("Rotor") or 
       model:FindFirstChild("MainRotor") or 
       model:FindFirstChild("TailRotor") or
       model:FindFirstChild("RotorHead") or
       model:FindFirstChild("Blades") then
        return true
    end
    
    return false
end

-- --- ENCONTRAR ASSENTO NO MODELO ---
local function encontrarAssento(modelo)
    if not modelo then return nil end
    
    local assento = modelo:FindFirstChildOfClass("VehicleSeat")
    if assento then return assento end
    
    assento = modelo:FindFirstChild("PilotSeat", true)
    if assento then return assento end
    
    for _, child in pairs(modelo:GetDescendants()) do
        if child:IsA("BasePart") then
            local nome = string.lower(child.Name)
            if string.find(nome, "seat") or 
               string.find(nome, "pilot") or 
               string.find(nome, "driver") or
               string.find(nome, "cockpit") then
                return child
            end
        end
    end
    
    return nil
end

-- --- BYPASS NETWORK OWNERSHIP ---
local function executarBypassNetworkOwnership(assentoAlvo)
    if bypassEmAndamento then return false end
    if aguardandoRetorno then return false end
    
    local meuHRP = character and character:FindFirstChild("HumanoidRootPart")
    local meuHum = character and character:FindFirstChildOfClass("Humanoid")
    
    if not meuHRP or not meuHum or not assentoAlvo then return false end
    if not assentoAlvo:IsA("BasePart") then return false end
    
    posicaoOriginal = meuHRP.CFrame
    
    local distancia = (meuHRP.Position - assentoAlvo.Position).Magnitude
    if distancia > 150 then
        return false
    end
    
    bypassEmAndamento = true
    
    task.spawn(function()
        local sucesso = false
        pcall(function()
            meuHRP.CFrame = assentoAlvo.CFrame
            task.wait(0.5)
            meuHum.Sit = false
            meuHum.Jump = true
            task.wait(0.2)
            task.wait(0.1)
            if posicaoOriginal then
                meuHRP.CFrame = posicaoOriginal
                sucesso = true
            end
        end)
        
        bypassEmAndamento = false
        aguardandoRetorno = false
    end)
    
    return true
end

-- --- FUNÇÃO PARA CRIAR O BOTÃO NA TELA ---
local function criarBotao()
    if botaoCriado then return actionButton end
    
    -- Verifica se já existe um botão
    local existingButton = playerGui:FindFirstChild("SelectMatterV4Button")
    if existingButton then
        existingButton:Destroy()
    end
    
    local button = Instance.new("TextButton")
    button.Name = "SelectMatterV4Button"
    button.Parent = playerGui
    button.Size = UDim2.new(0, 80, 0, 80)
    button.Position = UDim2.new(0.02, 0, 0.5, -40)
    button.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    button.BackgroundTransparency = 0
    button.BorderSizePixel = 0
    button.Text = "SELECT\nMATTER V4"
    button.TextColor3 = Color3.fromRGB(255, 255, 255)
    button.TextTransparency = 0
    button.Font = Enum.Font.Arcade
    button.TextScaled = true
    button.TextWrapped = true
    button.ZIndex = 10
    button.Visible = true
    button.Active = true
    button.Selectable = true
    
    local uiPadding = Instance.new("UIPadding")
    uiPadding.PaddingTop = UDim.new(0.08, 0)
    uiPadding.PaddingBottom = UDim.new(0.08, 0)
    uiPadding.PaddingLeft = UDim.new(0.08, 0)
    uiPadding.PaddingRight = UDim.new(0.08, 0)
    uiPadding.Parent = button
    
    local uiStroke = Instance.new("UIStroke")
    uiStroke.Color = Color3.fromRGB(255, 255, 255)
    uiStroke.Thickness = 3
    uiStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
    uiStroke.LineJoinMode = Enum.LineJoinMode.Miter
    uiStroke.Parent = button
    
    local uiCorner = Instance.new("UICorner")
    uiCorner.CornerRadius = UDim.new(0, 2)
    uiCorner.Parent = button
    
    botaoCriado = true
    
    -- Configura o clique do botão
    button.MouseButton1Click:Connect(function()
        if modoAtivo then
            resetarEstado()
        else
            modoAtivo = true
            button.BackgroundTransparency = 0.45
            button.TextTransparency = 0.2
        end
    end)
    
    return button
end

-- --- FUNÇÃO PARA ATUALIZAR TEXTO DO BOTÃO ---
local function atualizarTexto()
    if not botaoCriado then return end
    local button = playerGui:FindFirstChild("SelectMatterV4Button")
    if not button then return end
    
    if #objetosSelecionados > 0 then
        button.Text = "DESTROY\n(" .. #objetosSelecionados .. ")"
    else
        button.Text = "SELECT\nMATTER V4"
    end
end

-- --- RESETAR ESTADO E LIMPAR SELEÇÃO ---
local function resetarEstado()
    if conexaoAtaque then
        conexaoAtaque:Disconnect()
        conexaoAtaque = nil
    end
    objetosSelecionados = {}
    modoAtivo = false
    posicaoOriginal = nil
    bypassEmAndamento = false
    aguardandoRetorno = false
    
    local button = playerGui:FindFirstChild("SelectMatterV4Button")
    if button then
        button.BackgroundTransparency = 0
        button.TextTransparency = 0
    end
    atualizarTexto()
end

-- --- INJEÇÃO DOS MOTORES DE FORÇA BRUTA (FLING BALL) ---
local function aplicarMotoresFling(corpo)
    for _, child in pairs(corpo:GetChildren()) do
        if child:IsA("BodyVelocity") or child:IsA("BodyAngularVelocity") or child.Name == "BobsledSpinMotor" then
            child:Destroy()
        end
    end

    corpo.CanCollide = true
    corpo.Massless = false
    corpo.CustomPhysicalProperties = PhysicalProperties.new(100, 100, 0, 100, 100)
    
    local forcaMassiva = Vector3.new(math.huge * math.huge, math.huge * math.huge, math.huge * math.huge)

    local velocity = Instance.new("BodyVelocity")
    velocity.Name = "FlingPowerLinear"
    velocity.Velocity = Vector3.new(999999, 999999, 999999)
    velocity.MaxForce = forcaMassiva
    velocity.P = math.huge
    velocity.Parent = corpo  

    local angularVelocity = Instance.new("BodyAngularVelocity")
    angularVelocity.Name = "FlingPowerAngular"
    angularVelocity.AngularVelocity = Vector3.new(999999, 999999, 999999)
    angularVelocity.MaxTorque = forcaMassiva
    angularVelocity.P = math.huge
    angularVelocity.Parent = corpo
end

-- --- EXECUÇÃO DO ATAQUE EM MASSA ---
local function iniciarAtaqueMassa(alvoPlayer)
    local alvoChar = alvoPlayer.Character
    if not alvoChar or not alvoChar:FindFirstChild("HumanoidRootPart") then return end

    local alvoHum = alvoChar.Humanoid
    local meuHRP = character and character:FindFirstChild("HumanoidRootPart")

    if not meuHRP or #objetosSelecionados == 0 then return end

    for _, objeto in pairs(objetosSelecionados) do
        if objeto and objeto.Parent then
            aplicarMotoresFling(objeto)

            local nc = Instance.new("NoCollisionConstraint", objeto)
            nc.Part0 = objeto
            nc.Part1 = meuHRP

            if sethp then
                pcall(function() sethp(objeto, "NetworkOwner", lp) end)
            elseif objeto.SetNetworkOwner then
                pcall(function() objeto:SetNetworkOwner(lp) end)
            end
        end
    end

    local partesAlvo = {"HumanoidRootPart", "Head", "LowerTorso"}
    local indexParte = 1

    conexaoAtaque = RunService.Heartbeat:Connect(function()
        if not alvoChar.Parent or alvoHum.Health <= 0 or #objetosSelecionados == 0 then
            resetarEstado()
            return
        end

        local nomeParte = partesAlvo[indexParte]
        local p = alvoChar:FindFirstChild(nomeParte)

        if p then
            local offsetCurto = Vector3.new(0, 0, 0)
            if p.AssemblyLinearVelocity.Magnitude > 0.1 then
                offsetCurto = p.AssemblyLinearVelocity.Unit * 0.8
            end

            local tempo = tick() * 45 

            for i = #objetosSelecionados, 1, -1 do
                local objeto = objetosSelecionados[i]

                if objeto and objeto.Parent then
                    local angulo = tempo + (i * (math.pi / #objetosSelecionados))
                    local raio = 1 + (math.sin(tempo + i) * 0.5 + 0.5) 

                    local offsetOrbital = Vector3.new(
                        math.cos(angulo) * raio,
                        math.sin(angulo * 1.5) * raio, 
                        math.sin(angulo) * raio
                    )

                    local posicaoFinal = p.Position + offsetCurto + offsetOrbital
                    local direcaoGiro = CFrame.Angles(math.sin(tempo) * 3, math.cos(tempo) * 3, math.sin(tempo * 2) * 3)

                    objeto.CFrame = CFrame.new(posicaoFinal) * direcaoGiro
                    objeto.Velocity = Vector3.new(999999, 999999, 999999)
                    objeto.RotVelocity = Vector3.new(999999, 999999, 999999)
                else
                    table.remove(objetosSelecionados, i)
                    atualizarTexto()
                end
            end
        end

        indexParte = indexParte + 1
        if indexParte > #partesAlvo then indexParte = 1 end
    end)
end

-- --- FUNÇÃO PARA VERIFICAR SE O JOGADOR ESTÁ SEGURANDO A TOOL ---
local function verificarTool()
    local tool = lp.Character and lp.Character:FindFirstChildOfClass("Tool")
    if tool then
        -- Se estiver segurando uma tool, verifica se é a nossa
        if tool.Name == "SelectMatterV4" or tool.Name == "SELECT MATTER" or tool.Name == "SelectMatter" then
            if not botaoCriado then
                criarBotao()
                modoAtivo = true
                local button = playerGui:FindFirstChild("SelectMatterV4Button")
                if button then
                    button.BackgroundTransparency = 0.45
                    button.TextTransparency = 0.2
                end
            end
        else
            -- Se não for nossa tool, remove o botão
            if botaoCriado then
                local button = playerGui:FindFirstChild("SelectMatterV4Button")
                if button then button:Destroy() end
                botaoCriado = false
                resetarEstado()
            end
        end
    else
        -- Se não tiver tool, remove o botão
        if botaoCriado then
            local button = playerGui:FindFirstChild("SelectMatterV4Button")
            if button then button:Destroy() end
            botaoCriado = false
            resetarEstado()
        end
    end
end

-- --- MONITORAR MUDANÇAS NO CHARACTER PARA VERIFICAR A TOOL ---
lp.CharacterAdded:Connect(function(char)
    character = char
    -- Aguarda um pouco para a tool ser carregada
    task.wait(0.5)
    verificarTool()
end)

-- --- MONITORAR SE O JOGADOR ESTÁ SEGURANDO A TOOL ---
RunService.Heartbeat:Connect(function()
    if not character then return end
    verificarTool()
end)

-- --- CAPTURA DE CLIQUES NO MUNDO ---
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed or not modoAtivo then return end
    if bypassEmAndamento or aguardandoRetorno then return end
    if not botaoCriado then return end

    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        local pos = input.Position
        local unitRay = camera:ScreenPointToRay(pos.X, pos.Y)

        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Blacklist
        raycastParams.FilterDescendantsInstances = {character}

        local raycastResult = Workspace:Raycast(unitRay.Origin, unitRay.Direction * 50000, raycastParams)

        if raycastResult and raycastResult.Instance then
            local hitPart = raycastResult.Instance
            local model = hitPart:FindFirstAncestorOfClass("Model")

            -- CASO 1: Clicar em um jogador inimigo
            if model and model:FindFirstChild("Humanoid") then
                local tPlayer = Players:GetPlayerFromCharacter(model)

                if tPlayer and tPlayer ~= lp then
                    if #objetosSelecionados > 0 then
                        if conexaoAtaque then conexaoAtaque:Disconnect() end
                        iniciarAtaqueMassa(tPlayer)
                    end
                end

            -- CASO 2: Clicar em objetos/veículos físicos
            else
                local alvoFisico = nil
                local modeloVeiculo = hitPart:FindFirstAncestorOfClass("Model") or hitPart

                if hitPart.Anchored == false then
                    if hitPart:IsA("BasePart") then
                        alvoFisico = hitPart
                    elseif modeloVeiculo:IsA("Model") and modeloVeiculo.PrimaryPart then
                        alvoFisico = modeloVeiculo.PrimaryPart
                    else
                        alvoFisico = hitPart
                    end
                end

                if alvoFisico and not table.find(objetosSelecionados, alvoFisico) then
                    table.insert(objetosSelecionados, alvoFisico)
                    atualizarTexto()
                    print("Objeto selecionado: " .. (alvoFisico.Name or "sem nome"))

                    local modeloReal = alvoFisico:FindFirstAncestorOfClass("Model") or alvoFisico
                    
                    if ehHelicoptero(modeloReal) then
                        print("Helicóptero detectado! Buscando assento...")
                        
                        local assentoEncontrado = encontrarAssento(modeloReal)
                        
                        if assentoEncontrado then
                            print("Assento encontrado: " .. assentoEncontrado.Name)
                            aguardandoRetorno = true
                            local bypassExecutado = executarBypassNetworkOwnership(assentoEncontrado)
                            
                            if not bypassExecutado then
                                aguardandoRetorno = false
                                print("Falha ao executar bypass no assento")
                            else
                                print("Bypass em andamento para o helicóptero")
                            end
                        else
                            print("Nenhum assento encontrado no helicóptero")
                        end
                    end
                end
            end
        end
    end
end)

-- --- CRIA O BOTÃO INICIALMENTE SE O JOGADOR JÁ ESTIVER SEGURANDO A TOOL ---
task.wait(1)
verificarTool()

-- --- REGISTRO NO ADAPTADOR ---
_G.ScriptAdapter = _G.ScriptAdapter or {}
_G.ScriptAdapter.Queue = _G.ScriptAdapter.Queue or {}

local toolData = {
    Id = "SelectMatterV4_Tool",
    OnEquip = function()
        criarBotao()
        modoAtivo = true
        local button = playerGui:FindFirstChild("SelectMatterV4Button")
        if button then
            button.BackgroundTransparency = 0.45
            button.TextTransparency = 0.2
        end
    end,
    OnUnequip = function()
        resetarEstado()
        local button = playerGui:FindFirstChild("SelectMatterV4Button")
        if button then button:Destroy() end
        botaoCriado = false
    end
}

if _G.ScriptAdapter and _G.ScriptAdapter.RegisterTool then
    _G.ScriptAdapter:RegisterTool(toolData)
else
    table.insert(_G.ScriptAdapter.Queue, toolData)
end

print("SELECT MATTER V4 CORRIGIDO - Carregado com sucesso!")
